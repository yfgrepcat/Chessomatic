# How-to generate the graphs

Navigate at the root of the project and run the following command:

```bash
./.venv/bin/python project/experiments/analysis.py --worker-id p300_sf10_vs_sf8_basic --bandit-type basic_linucb
```